{
  "name": "flooring-quote-desk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Email intake engine for a US flooring contractor: the model extracts facts, deterministic code makes every decision",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prepare": "git config core.hooksPath .githooks || true",
    "test": "node --test",
    "extract": "node scripts/extract-code-nodes.js",
    "check-drift": "node scripts/extract-code-nodes.js --check",
    "deploy": "npm run test:logic && node --env-file=.env scripts/deploy.js && npm run extract && npm test",
    "test:logic": "node --test tests/decision-gate.test.js tests/invariants.test.js tests/signal-dictionaries.test.js tests/lookup-contract.test.js tests/extraction-contract.test.js tests/storage-contract.test.js tests/read-the-sheet.test.js tests/handoff-status.test.js tests/migration-names.test.js",
    "guards": "node scripts/guards.mjs",
    "ci:wait": "node scripts/ci-wait.mjs",
    "scan": "gitleaks detect --no-git --redact --config .gitleaks.toml -v",
    "db:check": "sh scripts/db-check.sh",
    "db:check:live": "sh scripts/db-check.sh --live",
    "db:round-trip": "node scripts/db-round-trip.mjs",
    "catalogue:round-trip": "node scripts/catalogue-round-trip.mjs",
    "conversation:round-trip": "node scripts/conversation-round-trip.mjs",
    "errors:round-trip": "node scripts/errors-round-trip.mjs",
    "migrate": "node --env-file=.env scripts/migrate.mjs",
    "migrate:apply": "node --env-file=.env scripts/migrate.mjs --apply"
  }
}
